[project]
name = "muto-agent"
version = "0.1.0"
description = "OpenAI 호환 LiteLLM Proxy에 붙는 간단한 Streamlit 챗봇"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "openai",
    "python-dotenv",
    "watchdog",
    "fastapi",
    "uvicorn",
    "httpx",
    "mcp",
]
